int a = 5;
int b = 1 + 2;
int d;
int id(int a){
	return a;
}

int PARAM = 5;

int foo(int a, int b){
	if(a < b){
		return a;	
	}
	else{
		return foo(a + -1, b);
	}
}

int main(){
	putchar(a+b+id(a + b));
	putchar(15);
	putchar(foo(4, 12));
	putchar(foo(10, 10));
	putchar(3 * 4 + 1 * 8);
	return 0;
}